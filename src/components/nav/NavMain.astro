---
import { shortTradesConst } from "@assets/info/estimateOptions";
---

<header class="flex items-center p-4 sticky top-0">
	<a href="/" class="text-xl font-bold capitalize">
		home upgrade services
	</a>
	<div class="flex gap-4 ml-auto items-center">
		{
			shortTradesConst.map((option) => (
				<a
					href={`/estimate/${option}/zip`}
					class="opacity-30 hover:opacity-100"
				>
					{" "}
					{option}{" "}
				</a>
			))
		}
		{
			/*
		<a href="/results" class="opacity-30 hover:opacity-100">
			results
		</a>
		*/
		}
	</div>
</header>
